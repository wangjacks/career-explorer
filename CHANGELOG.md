# Changelog

## [2.0.0] - 2026-09-03

### Added

- 三角色统一用户体系（admin / teacher / student），支持班级邀请码激活与教师-班级多对多关联
- 学生档案单路由多步表单，支持登录门、标签、词云、评价、形象、确认、完成，以及草稿暂存和提交截止时间
- 档案提交历史版本，支持学生查看/恢复、管理端查看与超限清理
- 对象存储抽象与 S3 兼容后端，支持私有读写、签名 URL、多后端与本地迁移
- 媒体资源管理面板，支持文件枚举、孤儿检测、批量清理、缩略图维护
- 操作审计日志，覆盖登录、管理、导出、备份等关键操作，教师仅可见本人记录
- Excel 导出支持原生单元格图片与浮动图片
- 班级邀请二维码与分享海报，配套路由级测试（#102）
- UI/UX 品牌化重设计，新增暗色主题、侧边栏导航、学生面板、教师面板与管理面板
- 备份恢复纳入档案提交历史、审计日志与存储后端配置（不含凭据）

### Changed

- 数据库升级为统一 `users` / `classes` / `teacher_classes` / `tags` 核心表，并扩展配置、审计、存储后端与提交历史表
- 认证切换为 JWT + bcrypt，并引入 `proxy` 角色权限中间件
- 文件上传改为服务端压缩、大小限制与 SVG 拒绝
- 页面全站 HTML 禁缓存，字体切换为 Noto Sans SC
- 硬编码颜色迁移至语义设计 token，次要文本统一为 `text-muted`（#135）

### Fixed

- 跨数据库备份恢复、外键约束、空字符串写入 INT 等兼容性问题
- 主题闪烁、页面溢出、固定底栏、用户菜单遮挡等 UI 问题
- 媒体扫描、缩略图、审计搜索等若干缺陷
- 管理面板数据操作：数据库配置同步、统计加载失败重试、学生更新班级校验与批量错误提示、档案分页刷新与可见行选择（#132）
- 暗色主题覆盖缺口：安装页、错误边界、图片上传框、词云与面板组件（#133）
- 前端无障碍缺陷：键盘可达性、ARIA 语义、页脚/步骤/词云（#134）
- 标签统计聚合在标签文本直存格式下的回归，按名称聚合并兼容旧 ID 引用（#141）
- 关于页学生流程步骤描述（#130）

## [2.0.0-beta.2] - 2026-08-31

### Added

- 三角色统一用户体系（admin / teacher / student），支持班级邀请码激活与教师-班级多对多关联
- 学生档案单路由多步表单，支持登录门、标签、词云、评价、形象、确认、完成，以及草稿暂存和提交截止时间
- 档案提交历史版本，支持学生查看/恢复、管理端查看与超限清理
- 对象存储抽象与 S3 兼容后端，支持私有读写、签名 URL、多后端与本地迁移
- 媒体资源管理面板，支持文件枚举、孤儿检测、批量清理、缩略图维护
- 操作审计日志，覆盖登录、管理、导出、备份等关键操作，教师仅可见本人记录
- Excel 导出支持原生单元格图片与浮动图片
- UI/UX 品牌化重设计，新增暗色主题、侧边栏导航、学生面板、教师面板与管理面板
- 备份恢复纳入档案提交历史、审计日志与存储后端配置（不含凭据）

### Changed

- 数据库升级为统一 `users` / `classes` / `teacher_classes` / `tags` 核心表，并扩展配置、审计、存储后端与提交历史表
- 认证切换为 JWT + bcrypt，并引入 `proxy` 角色权限中间件
- 文件上传改为服务端压缩、大小限制与 SVG 拒绝
- 页面全站 HTML 禁缓存，字体切换为 Noto Sans SC

### Fixed

- 跨数据库备份恢复、外键约束、空字符串写入 INT 等兼容性问题
- 主题闪烁、页面溢出、固定底栏、用户菜单遮挡等 UI 问题
- 媒体扫描、缩略图、审计搜索等若干缺陷

## [2.0.0-beta.1] - 2026-08-24

### Added

- v2.0.0 基础架构：统一 `users` / `classes` / `teacher_classes` / `tags` 四表设计，支持 admin / teacher / student 三角色
- 学生账户激活流程：学号 + 姓名 + 班级邀请码三要素核验，两步单页完成设置密码
- 教师面板与管理面板：教师班级管理、学生管理、班级概览、教师账户管理、标签管理
- 学生面板：档案查询、信息通览与就地修改，图片延迟上传与预览
- 标签体系重构：预设标签层级管理、批量导入、自定义标签、数量上限配置
- 学生档案表单重构为单路由多步流程，支持草稿暂存与提交截止时间
- UI/UX 品牌化改版：深绿品牌色、暗色主题、侧边栏导航、表单进度动效
- 字体切换为 Noto Sans SC，并支持 `FONT_CDN_PREFIX` 镜像前缀

### Changed

- 认证体系改为 JWT + bcrypt + `proxy` 角色权限中间件
- 管理端 API 前缀统一为 `/api/manage/*`，共享 API 迁移至 `/api/shared/*`
- 数据库与路由结构全面重构，移除旧注册、旧表单多页与快速提交链路

### Fixed

- 上传区居中、草稿恢复、会话状态刷新、页面溢出、用户菜单遮挡等 UI 与交互问题
- SQLite 路径校验、Turbopack 构建兼容、CodeQL 告警等安全与构建问题

## [1.0.0] - 2026-07-09

### Added

- 职业标签选择（兴趣/技能/性格三维度）
- 自定义标签输入
- 实时词云可视化展示
- 评价词云上传页面
- 头像上传与预览
- 学号验证（12 位数字）
- 已有填写记录检测与恢复
- 表单状态 localStorage 持久化
- 管理员面板（登录/数据概览/数据源设置/学生管理/数据导出）
- 数据导出（Excel/CSV，支持筛选/列选择/图片打包）
- 批量学生导入（标题行自动识别）
- 首次安装引导页面
- MySQL 数据库支持
- 数据源配置（SQLite/MySQL 切换）

### Changed

- 从微信小程序重构为 Next.js Web 应用
- 数据库从 SQLite 迁移至 MySQL
- 管理页面改为 Tab 导航

### Fixed

- 提交时间未加时区偏移
- 生产模式下上传图片无法显示
- 恢复记录后跳转页面错误
- 重复上传同名图片时浏览器缓存未更新
